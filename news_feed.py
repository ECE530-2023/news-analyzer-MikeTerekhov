#def uploader():
#    file_name = input("Enter the name of the document you are uploading: ")

#def checker():
#    return "Use same checker for upload() from secure_file_upload"

# return and present all data aquired from scrape using NLP
def presentation_interface(all_input_data):
    if len(all_input_data) == 0:
        return False
    else:
        return True
